To follow along, you need to have git, terraform and awscli installed on your local machine.
You need to set up your AWS `access keys` and s`ecret access keys`
Windows user can install wsl as showed in the wsl-install.md file.
